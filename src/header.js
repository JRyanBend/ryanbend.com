import React from "react";

export default React.createClass({
    render: function() {
        return (
            <div className="header">
            <img src="/src/code.png" />
	       		<h2>RYANBEND.COM</h2>
	       		<p>If you're seeing this, React is installed and working!</p>
	   		</div>
        );
    },
});
